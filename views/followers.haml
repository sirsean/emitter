
%h2="Followers"

%p="#{@user["username"]} has #{@followers.length} follower#{"s" unless @followers.length == 1}"

#user_list
    - @followers.each do |follower|
        = display_user_in_list(follower)

